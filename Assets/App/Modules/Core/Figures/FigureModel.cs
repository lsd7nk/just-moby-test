using UnityEngine;

namespace App.Core
{
    public struct FigureModel
    {
        public readonly Color Color;

        public FigureModel(Color color)
        {
            Color = color;
        }
    }
}